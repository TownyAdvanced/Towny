before_install:
    # Make sure SDKMAN is addressable and up-to-date.
    - source "$HOME/.sdkman/bin/sdkman-init.sh"
    - sdk update
    # Use Eclipse Temurin 25 builds
    - sdk install java 25-tem
    - sdk use java 25-tem
